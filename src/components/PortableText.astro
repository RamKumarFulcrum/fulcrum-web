---
import { PortableText as PT } from 'astro-portabletext';

interface Props {
  value: any;
}

const { value } = Astro.props;
---

{value ? (
  <div class="prose">
    <PT value={value} />
  </div>
) : (
  <p>No content available.</p>
)}
